import { PuppeteerOptions, PuppeteerOptionsModel } from '@models'

export const puppeteerOptions: PuppeteerOptionsModel  = {
  PAGE_URL: PuppeteerOptions.PAGE_URL,
  TIMEOUT: PuppeteerOptions.TIMEOUT,
  USER_AGENT: PuppeteerOptions.USER_AGENT,
}
